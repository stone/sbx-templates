-- Options passed to require("lazy").setup(spec, opts).
return {
  -- Plugins without an explicit lazy trigger (cmd/keys/event/ft) load at startup.
  defaults = { lazy = false },

  ui = {
    icons = {
      ft = "",
      lazy = "󰂠 ",
      loaded = "",
      not_loaded = "",
    },
  },

  performance = {
    rtp = {
      disabled_plugins = {
        "gzip",
        "matchit",
        "matchparen",
        "netrwPlugin",
        "tarPlugin",
        "tohtml",
        "tutor",
        "zipPlugin",
      },
    },
  },
}
